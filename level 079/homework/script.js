// 2)
// ფუნქცია არის კოდის ბლოკი, რომელიც ასრულებს კონკრეტულ დავალებას.
// ფუნქციებს ვიყენებთ იმისთვის, რომ ერთი და იგივე კოდი ბევრჯერ არ დავწეროთ და საჭიროების შემთხვევაში მარტივად გამოვიძახოთ.
// ფუნქციის შექმნის ეტაპები:
// 1. ვწერთ function საკვანძო სიტყვას.
// 2. ვაძლევთ ფუნქციას სახელს.
// 3. ფრჩხილებში ვწერთ პარამეტრებს (თუ საჭიროა).
// 4. ფიგურულ ფრჩხილებში ვწერთ შესასრულებელ კოდს.
// 5. ფუნქციას ვიძახებთ მისი სახელით და საჭირო არგუმენტებით.
// პარამეტრები არის ცვლადები, რომლებიც ფუნქციის შექმნისას იწერება.
// არგუმენტები არის რეალური მნიშვნელობები, რომლებიც ფუნქციის გამოძახებისას გადაეცემა.

// 3)
function sum(num1, num2) {
    return num1 + num2;
}

console.log(sum(10, 15));

// 4)
function checkEven(num) {
    if (num % 2 === 0) {
        console.log("რიცხვი ლუწია");
    } else {
        console.log("რიცხვი კენტია");
    }
}

checkEven(8);
checkEven(7);

// 5)
function square(num) {
    return num ** 2;
}

console.log(square(6));

// 6)
function toUpper(text) {
    return text.toUpperCase();
}

console.log(toUpper("javascript"));

// 7)
function fullName(name, surname) {
    console.log("მომხმარებლის სახელია " + name + " ხოლო გვარია " + surname + ".");
}

fullName("Demetre", "Gelashvili");